# Library feature

Library: recent/favorites generation browser and prompt viewer.

Edit this folder independently. Enable/disable the feature with the corresponding `FEATURE_*_ENABLED` setting in `.env`.
